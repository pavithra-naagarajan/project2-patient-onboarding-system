import { PatientAppointment } from './patient-appointment';

describe('PatientAppointment', () => {
  it('should create an instance', () => {
    expect(new PatientAppointment()).toBeTruthy();
  });
});
